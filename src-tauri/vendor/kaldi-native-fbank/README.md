# Vendored kaldi-native-fbank

This directory contains `kaldi-native-fbank` 0.1.0 from
<https://github.com/RustedBytes/kaldi-native-fbank>, licensed under MIT.

The unused `rand_distr`, `anyhow`, `thiserror`, and `log` manifest dependencies
are omitted. The source is otherwise unchanged.
